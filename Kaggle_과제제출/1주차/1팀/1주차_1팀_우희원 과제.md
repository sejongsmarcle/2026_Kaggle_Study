```python

import numpy as np #넘파이 판다스 가져오기
import pandas as pd
import warnings #경고 모듈 가져오기
warnings.filterwarnings("ignore") #경고는 무시 오류는 띄움
train_df = pd.read_csv("./data/train.csv") #훈련, 테스트 데이터 가져오기
test_df = pd.read_csv("./data/test.csv")
submission = pd.read_csv("./data/gender_submission.csv") # 제출 답안지 양식 가져오기
train_df #훈련데이터 확인
test_df #테스트데이터 확인
submission #제출용 답안지 확인
import random #랜덤 모듈 가져오기
np.random.seed(1234) #넘파이, 파이썬의 시드 1234로 고정
random.seed(1234)

print(train_df.shape) #훈련데이터 크기 확인
print(test_df.shape) #테스트데이터 크기 확인
pd.set_option("display.max_columns",50) #데이터프레임 출력시 최대범위 설정
pd.set_option("display.max_rows",50) #행, 열 각각 50개 최대
train_df.head() #훈련데이터 앞부분만 보기(기본5개행)
test_df.head() #테스트 데이터 앞부분만 보기(기본5개행)
print(np.__version__) #넘파이, 판다스의 버전 확인
print(pd.__version__)
train_df.dtypes #훈련 데이터의 열별 자료형 확인
train_df.describe() #훈련 데이터 숫자형 열의 기초 통계량 확인
test_df.describe() #테스트 데이터 숫자형 열의 기초 통계량 확인
train_df["Sex"].value_counts() # 성별 승객수 확인
train_df["Embarked"].value_counts() #탑승 항구별 승객 수 확인
train_df["Cabin"].value_counts() #객실 등급별 승객 수 확인
train_df.isnull().sum() #훈련데이터 열별 결측치
test_df.isnull().sum() #테스트데이터 열별 결측치 확인

import matplotlib #멧플롯립 라이브러리 가져오기
print(matplotlib.__version__) #멧플롯립 버전 확인
%matplotlib inline # 그래프를 코드 셀 바로아래 출력하도록 설정
import matplotlib.pyplot as plt #멧플롯립의 그래프 작성 기능
import seaborn as sns #통계 그래프용 모듈 시본 가져오기
plt.style.use('ggplot') #ggplot 스타일 사용
train_df[["Embarked","Survived","PassengerId"]] #탑승항구, 생존여부, 승객번호 출력
train_df[["Embarked","Survived","PassengerId"]].dropna() #탑승항구,생존여부, 승객번호 불러와서 dropna로 결측치 있는부분만 지우기
train_df[["Embarked","Survived","PassengerId"]].dropna().groupby(["Embarked","Survived"]).count() #탑승항구, 생존여부 기반 그룹화하고 승객 수 세기
embarked_df = train_df[["Embarked","Survived","PassengerId"]].dropna().groupby(["Embarked","Survived"]).count().unstack() # 생존여부의 표시를 행>열
embarked_df #데이터 확인
embarked_df.plot.bar(stacked=True) #embarked_df 데이터를 막대그래프로 표시
embarked_df["survived_rate"]=embarked_df.iloc[:,0]/(embarked_df.iloc[:,0] + embarked_df.iloc[:,1]) #항구별 생존률 계산해서 새 열에 저장
embarked_df #embarked_df데이터 확인
sex_df = train_df[["Sex","Survived","PassengerId"]].dropna().groupby(["Sex","Survived"]).count().unstack() #성별, 생존여부, 승객번호 데이터 선택해
#결측치 제거, 성별/생존여부 그룹화 후 승객 수 세고 unstack으로 생존 여부를 열로 배치
sex_df.plot.bar(stacked=True) #sex_df데이터를 막대그래프 출력
ticket_df = train_df[["Pclass","Survived","PassengerId"]].dropna().groupby(["Pclass","Survived"]).count().unstack() #티켓 등급, 생존 여부,승객번호 
#선택해서 결측치 제거, 티켓등급/생존여부 그룹화해서 승객 수 세고 unstack으로 생존 여부 열로 배치
ticket_df.plot.bar(stacked=True) #만들어진 ticket_df 데이터 막대그래프 출력
plt.hist(x=[train_df.Age[train_df.Survived==0], train_df.Age[train_df.Survived==1]], bins=8  , histtype='barstacked', label=["Death","Survived"])
#사망자, 생존자 나이 데이터에서 나이를 8개 구간으로 나누고 사망-생존자 막대그래프 쌓기, 범례명 Death, Survived로 설정하고 히스토그램 출력
plt.legend() #범례 출력
train_df_corr = pd.get_dummies(train_df, columns=["Sex"],drop_first=True) #문자 형태인 성별 데이터를 숫자 형태로 변환(더미 변수화)
train_df_corr = pd.get_dummies(train_df_corr, columns=["Embarked"]) #문자형태인 승선 항구도 숫자 열로 변환해 숫자형태로 변환
train_df_corr.head() #더미변수화 된 데이터 앞 5개행 확인
train_corr = train_df_corr.corr() #숫자형 열 사이의 상관계수를 계산,train_corr에 저장
train_corr #train_corr 상관행렬 출력c
plt.figure(figsize=(9, 9)) #9*9인치 크기
sns.heatmap(train_corr, vmax=1, vmin=-1, center=0, annot=True) #상관행렬을 색깔로 표현한 히트맵 생성, 색상범위 최대1 최소-1,중심값=0, 각 칸에 상관계수 출력

all_df = pd.concat([train_df, test_df],sort=False).reset_index(drop=True) #훈련데이터 테스트데이터 세로로 연결해서 all_df에 저장
all_df #데이터 출력
all_df.isnull().sum() #all_df의 열별 결측치개수 확인
Fare_mean = all_df[["Pclass","Fare"]].groupby("Pclass").mean().reset_index()
#객실등급,요금 선택후 객실등급별 그룹화 및 평균요금 계산, Fare_mean에 저장
Fare_mean.columns = ["Pclass","Fare_mean"] #Fare_mean 데이터프레임 열 이름 변경
Fare_mean #Fare_mean 출력
all_df = pd.merge(all_df, Fare_mean, on="Pclass",how="left") #all_df, Fare_mean을 Pclass기준 결합
all_df.loc[(all_df["Fare"].isnull()), "Fare"] = all_df["Fare_mean"] #요금이 빈 승객은 평균요금으로 채우기
all_df = all_df.drop("Fare_mean",axis=1) #Fare_mean열 삭제
all_df["Name"].head() #승객이름 위 5개행 출력
name_df = all_df["Name"].str.split("[,.]",2,expand=True) #쉼표, 마침표 기준 최대 두번 승객 이름 분리해 name_df에 저장
name_df.columns = ["family_name","honorific","name"] #name_df의 열 이름을 성,호칭,이름으로 설정
name_df #출력
name_df["family_name"] =name_df["family_name"].str.strip() #각 문자열 앞뒤 불필요한 공백 제거
name_df["honorific"] =name_df["honorific"].str.strip()
name_df["name"] =name_df["name"].str.strip()
name_df["honorific"].value_counts() #honorific에 각 호칭이 몇번등장하는지 세기
all_df = pd.concat([all_df, name_df],axis=1) #all_df, name_df가로로 결합 
all_df #확인
plt.figure(figsize=(18, 5)) #크기 18*5
sns.boxplot(x="honorific", y="Age", data=all_df) #가로축엔 호칭 세로축엔 나이 > 박스플롯으로 출력
all_df[["Age","honorific"]].groupby("honorific").mean() #나이,호칭 선택해서 호칭별로 그룹화하고 평균 나이 계산
train_df = pd.concat([train_df,name_df[0:len(train_df)].reset_index(drop=True)],axis=1) #훈련데이터 부분 선택해 훈련데이터에 추가
test_df = pd.concat([test_df,name_df[len(train_df):].reset_index(drop=True)],axis=1)#테스트데이터 부분 선택해서 테스트데이터에 추가
honorific_df = train_df[["honorific","Survived","PassengerId"]].dropna().groupby(["honorific","Survived"]).count().unstack() #호칭별 생존여부
honorific_df.plot.bar(stacked=True)#막대그래프 출력
honorific_age_mean = all_df[["honorific","Age"]].groupby("honorific").mean().reset_index() #호칭별 평균나이 계산, 저장
honorific_age_mean.columns = ["honorific","honorific_Age"] #열 이름 변경
all_df = pd.merge(all_df, honorific_age_mean, on="honorific", how="left") #호칭 기준 평균나이 매칭
all_df.loc[(all_df["Age"].isnull()), "Age"] = all_df["honorific_Age"] #비어있는 나이는 평균나이로 대체
all_df = all_df.drop(["honorific_Age"],axis=1) #임시 평균나이 열 삭제
all_df["family_num"] = all_df["Parch"] + all_df["SibSp"] #부모/자녀, 형제자매 더해 family_num 에 저장
all_df["family_num"].value_counts() #동승 가족 수별 승객수 확인
all_df.loc[all_df["family_num"] ==0, "alone"] = 1 #동승 0인 alone값을 1로설정
all_df["alone"].fillna(0, inplace=True) #나머지 alone결측치는 0으로 설정
all_df = all_df.drop(["PassengerId","Name","family_name","name","Ticket","Cabin"],axis=1) #사용하지 않는 열들 삭제
all_df.head() #앞 5개행 출력
categories = all_df.columns[all_df.dtypes == "object"] #자료형 열들 찾아 출력
print(categories)
all_df.loc[~((all_df["honorific"] =="Mr") | #많이 사용된 4개호칭 이외는 other로 설정
    (all_df["honorific"] =="Miss") |
    (all_df["honorific"] =="Mrs") |
    (all_df["honorific"] =="Master")), "honorific"] = "other"
all_df.honorific.value_counts() #호칭별 승객수 확인
from sklearn.preprocessing import LabelEncoder #사이킷런에서 LabelEncoder불러기
all_df["Embarked"].fillna("missing", inplace=True) #탑승항구 비어있는 값을 missing으로변경
all_df.head() #앞 5개행 확인
for cat in categories: #범주형 열을 숫자로 변환해주기
    le = LabelEncoder()
    print(cat)
    if all_df[cat].dtypes == "object":    
        le = le.fit(all_df[cat])
        all_df[cat] = le.transform(all_df[cat])
all_df.head() #앞 5개행 확인
train_X = all_df[~all_df["Survived"].isnull()].drop("Survived",axis=1).reset_index(drop=True) #정답 열 제거하고 훈련데이터로 설정
train_Y = train_df["Survived"] #정답은 다른 훈련 데이터로 설정
test_X = all_df[all_df["Survived"].isnull()].drop("Survived",axis=1).reset_index(drop=True)#정답 여부가 비어있는 것들은 테스트 데이터로 설정

```
